class CurrencyRepository {
    getRate = (from, to) => { }
    setRate = (from, to, rate) => { }
    deleteRate = (from, to) => { }
    loadRates = () => { }
}

module.exports = CurrencyRepository