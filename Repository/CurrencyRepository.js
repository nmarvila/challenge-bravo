class CurrencyRepository {
    getRate = (from, to) => { }
    setRate = (from, to, rate, isCustomRate) => { }
    deleteRate = (from, to) => { }
    loadRates = () => { }
    getRates = () => { }
}

module.exports = CurrencyRepository